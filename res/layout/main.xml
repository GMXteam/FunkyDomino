<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:layout_width="fill_parent" 
                android:layout_height="fill_parent" 
                android:orientation="vertical">	
    
    <Button android:id="@+id/new_game_button"		
            android:layout_centerVertical="true"
            android:layout_centerHorizontal="true"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:onClick="onNewGameClick"
            android:text="Nouvelle partie" />

    <Button android:id="@+id/funky_domino_button"			
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_above="@id/new_game_button"
            android:layout_centerHorizontal="true"
            android:onClick="onPlayClick"
            android:text="Jouer" />

    <Button android:id="@+id/load_game_button"
            android:layout_centerHorizontal="true"	
            android:layout_below="@id/new_game_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:onClick="onLoadGameClick"
            android:text="Charger une partie" />
	
</RelativeLayout>